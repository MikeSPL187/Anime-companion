enum AnimeType { tv, movie, ova, ona, special, unknown }

enum AnimeSeason { winter, spring, summer, fall }

enum PublishDay {
  monday,
  tuesday,
  wednesday,
  thursday,
  friday,
  saturday,
  sunday,
}

enum AgeRating { g, pg, pg13, r, rPlus, rx, unknown }

enum LibraryStatus { watching, planned, completed, postponed, dropped }

enum AppThemeMode { system, light, dark }

enum RefreshBehavior { staleWhileRevalidate, manual }
